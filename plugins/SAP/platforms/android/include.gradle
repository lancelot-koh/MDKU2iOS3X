println '---------------------------INCLUDE.GRADLE---------------------------------'
println "$projectDir"

android {
    compileOptions {
        sourceCompatibility 1.8
        targetCompatibility 1.8
    }
}

buildscript {
    def versionsGradle = file("../../../../AndroidStudioProjects/MDKClient/versions.gradle")
    if (versionsGradle.exists()) {
        apply from: versionsGradle.getAbsolutePath()
    } else {
        def currentFileDir = buildscript.sourceFile.getParent()
        apply from: "$currentFileDir/versions.gradle"
    }
}

dependencies {
    implementation deps.android_lifecycle
    implementation deps.fiori
    implementation deps.onboarding
    implementation deps.odata
    implementation deps.offlineOData
    implementation deps.foundation
    implementation deps.tony19_logback
    implementation deps.slf4j_api
    implementation deps.slf4j_jul
    implementation deps.firebase
    implementation deps.support_compat
}
